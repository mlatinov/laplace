mod codegen;
mod docs;
mod manifest;
mod parser;
mod resolve;

fn main() {
    // TODO(Task 5): wire up clap subcommands (build, install, add, update, doc)
    eprintln!("laplace: CLI not implemented yet (see laplace-project-plan.md, Task 5)");
    std::process::exit(1);
}
